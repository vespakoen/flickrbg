{
  "name": "flickrbg",
  "version": "0.0.1",
  "description": "Take a random image from a photoset in flickr and set it as the background.",
  "main": "index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/vespakoen/flickrbg.git"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "watch": "babel index.src.js --stage 0 --watch -o index.js",
    "start": "node index.js | sh"
  },
  "author": "Koen Schmeets",
  "license": "MIT",
  "dependencies": {
    "lodash.random": "^3.0.1",
    "node-fetch": "^1.3.2",
    "xml2js": "^0.4.12"
  },
  "devDependencies": {
    "babel": "^5.8.23"
  }
}
